// character literal (single quotes)
Console.WriteLine('b');

// String
Console.WriteLine("Hello World!");

// Integer literal (int literal)
Console.WriteLine(123);

// Floating Point literals (decimal)
// append the letter F (literal suffix) after the number
Console.WriteLine(0.25F);
// Float 6-9 digits
// Double 15-17 digits
Console.WriteLine(2.625);
// Decimal 28-29 digits
// To create a decimal literal, append the letter m after the number
Console.WriteLine(12.39816m);

// Boolean (bool literal)
Console.WriteLine(true);
Console.WriteLine(false);

// The main takeaway is that there are many data types, but you'll focus on just a few for now:

// string for words, phrases, or any alphanumeric data for presentation, not calculation
// char for a single alphanumeric character
// int for a whole number
// decimal for a number with a fractional component
// bool for a true/false value
